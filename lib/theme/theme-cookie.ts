/** Cookie read by inline `beforeInteractive` script + ThemeProvider (iOS PWA chrome). */
export const THEME_COOKIE_NAME = "soilmates-color-theme";

export type ThemeCookieValue = "light" | "dark" | "system";
